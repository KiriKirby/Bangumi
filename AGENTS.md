# Bangumi Agent Notes

## Discovery Type Labels

- 发现页卡片上的类型标签必须使用条目的真实 `SubjectType`，禁止写死成“动画”。
- 类型文案统一使用：
  - `Anime` -> `动画`
  - `Book` -> `书籍`
  - `Music` -> `音乐`
  - `Game` -> `游戏`
  - `Real` -> `三次元`

## Discovery Type Colors

- 发现页类型标签颜色统一按 `czy0729.Bangumi` 的主题色槽位映射：
  - `动画` -> `rgb(254, 138, 149)` / `#FE8A95`
  - `书籍` -> `rgb(13, 183, 243)` / `#0DB7F3`
  - `音乐` -> `rgb(50, 200, 64)` / `#32C840`
  - `游戏` -> `rgb(254, 190, 88)` / `#FEBE58`
  - `三次元` -> `rgb(1, 173, 145)` / `#01AD91`

- 后续新增发现页卡片、列表、徽标时，类型色继续复用这套映射，不要另起一套近似颜色。
